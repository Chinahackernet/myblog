## 常用API

<table class="lake-table" style="width: 736px;"><colgroup><col span="1" width="170" /><col span="1" width="566" /></colgroup><tbody><tr style="height: 33px;"><td>查询项目<p data-lake-id="520f42f3293818f927861ebbd5b15da4_p_0"><br /></p></td><td><p data-lake-id="99eb8800db747256de07a630449fb054_p_0">/api/projects/search?projects=${projectName}</p></td></tr><tr style="height: 33px;"><td><p data-lake-id="e0363a4bde8d30a5b842d287ca8bd143_p_0">创建项目</p></td><td><p data-lake-id="99eb8800db747256de07a630449fb054_p_0">/api/projects/create?name=${projectName}&amp;project=${projectName}</p></td></tr><tr style="height: 33px;"><td><p data-lake-id="489860d4ba76273e75adc2ff37bff486_p_0">更新语言规则集</p></td><td rowspan="1" colspan="1">/api/qualityprofiles/add_project?language=${language}&amp;qualityProfile=${qualityProfile}&amp;project=${projectName}</td></tr><tr style="height: 33px;"><td colspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;"><p data-lake-id="7456b68bee5ec0f314313186b3f07b03_p_0">项目授权</p></td><td colspan="1" rowspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;">/api/permissions/apply_template?projectKey=${projectKey}&amp;templateName=${templateName}</td></tr><tr style="height: 33px;"><td colspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;"><p data-lake-id="a20d224c568e48b9d67847a2c66a8c01_p_0">更新质量阈</p></td><td colspan="1" rowspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;">/api/qualitygates/select?projectKey=${projectKey}&amp;gateId=${gateId}</td></tr><tr style="height: 33px;"><td colspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;"><p data-lake-id="dc6bc1913022de3f155dcff7ce581e2f_p_0">获取质量阈ID</p></td><td colspan="1" style="vertical-align: top; background-color: #FFFFFF; color: #262626;"><p data-lake-id="99eb8800db747256de07a630449fb054_p_0">/api/qualitygates/show?name=${qualityName}</p></td></tr></tbody></table>

## 查询项目

```groovy
// 获取sonar项目，判断项目是否存在
def SearchProject(projectName){
    def apiUrl = "projects/search?projects=${projectName}"
    // 发请求
    response = HttpReq("GET",apiUrl,"")
    // 对返回的文本做JSON解析
    response = readJSON text: """${response.content}"""
    // 获取total字段，该字段如果是0则表示项目不存在,否则表示项目存在
    result = response["paging"]["total"]
    // 对result进行判断
    if (result.toString() == "0"){
        return "false"
    }
    return "true"
}
```

  

## 创建项目

```groovy
// 创建sonar项目
def CreateProject(projectName){
    def apiUrl = "projects/create?name=${projectName}&project=${projectName}"
    // 发请求
    response = HttpReq("POST",apiUrl,"")
    println(response)
}
```

## 配置项目质量规则

```groovy
// 配置项目质量规则
def ConfigQualityProfiles(language,qualityProfile,projectName){
    def apiUrl = "qualityprofiles/add_project?language=${language}&qualityProfile=${qualityProfile}&project=${projectName}"
    // 发请求
    response = HttpReq("POST",apiUrl,"")
    println(response)
}
```

## 获取质量阈ID

```groovy
// 获取质量阈ID
def GetQualityGateId(gateName){
    def apiUrl = "qualitygates/show?name=${gateName}"
    // 发请求
    response = HttpReq("GET",apiUrl,"")
    // 对返回的文本做JSON解析
    response = readJSON text: """${response.content}"""
    // 获取total字段，该字段如果是0则表示项目不存在,否则表示项目存在
    result = response["id"]
    return result
}
```

## 配置质量阈规则

```groovy
// 更新质量阈规则
def ConfigQualityGate(projectKey,gateName){
    // 获取质量阈id
    gateId = GetQualityGateId(gateName)
    apiUrl = "qualitygates/select?projectKey=${projectKey}&gateId=${gateId}"
    // 发请求
    response = HttpReq("POST",apiUrl,"")
    println(response)
}
```

  

## 在项目中应用

修改[Jenkinsfile](https://gitee.com/jokerbai/jenkins-shareLibrary/blob/master/ci.Jenkinsfile)

```groovy
		stage("CodeScanner"){
			steps{
				timeout(time:30,unit:'MINUTES'){
					script{
						// 在扫描之前判断sonar项目是否存在
						res = sonarapi.SearchProject("${JOB_NAME}")
						if (res == "false"){
							// 表示项目不存在，需要新增项目
							tools.PrintMes("新增Sonar项目${JOB_NAME}",'green')
							sonarapi.CreateProject("${JOB_NAME}")
							// 配置项目质量规则，这里是写死的，后期再修改
							tools.PrintMes("配置项目${JOB_NAME}质量规则",'green')
							sonarapi.ConfigQualityProfiles("java","java-test","${JOB_NAME}")
							// 配置质量阈
							tools.PrintMes("配置项目${JOB_NAME}质量阈",'green')
							sonarapi.ConfigQualityGate("${JOB_NAME}","demo")
						}
						tools.PrintMes("代码扫描",'green')
						sonar.SonarScan("pre","${JOB_NAME}","${JOB_NAME}","src")
					}
				}
			}
		}
....
```

然后新建一个项目，执行pipeline，查看日志如下：

![image.png](assets/devops与交付/4.9.3、SonarQube_API操作/4.9.3、SonarQube_API操作-1.png)