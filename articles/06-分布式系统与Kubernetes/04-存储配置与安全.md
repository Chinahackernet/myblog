# Kubernetes 存储、配置与安全

ConfigMap 保存非敏感配置，Secret 仅提供编码，不等于加密边界；敏感数据应结合 KMS、外部 Secret 管理和 RBAC。PV/PVC 解耦存储供给与工作负载。

```bash
kubectl get pv,pvc,secret,configmap -A
kubectl auth can-i get secrets -n <NS> --as <USER>
```

生产基线包括 Pod Security、NetworkPolicy、ServiceAccount 最小权限、镜像扫描、审计日志和备份恢复演练。
